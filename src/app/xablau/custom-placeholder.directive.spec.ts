import { CustomPlaceholderDirective } from './custom-placeholder.directive';

describe('CustomPlaceholderDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomPlaceholderDirective();
    expect(directive).toBeTruthy();
  });
});
